model Cart{
    userId Int
    productId Int
    createdAt DateTime @default(now())

    @@id([userId,productId])

    userIdProduct UserProfile @relation(fields: [userId], references: [userId], onDelete: Cascade)
    cartProduct Product @relation(fields: [productId], references: [productId], onDelete: Cascade)
}