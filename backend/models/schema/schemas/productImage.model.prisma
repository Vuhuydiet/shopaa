model ProductImage {
    imageId   String @id
    productId Int
    order     Int    @default(autoincrement())

    image   Image   @relation(fields: [imageId], references: [publicId], onDelete: Restrict)
    product Product @relation(fields: [productId], references: [productId], onDelete: Restrict)
}
