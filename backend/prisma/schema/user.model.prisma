model User {
    userId      Int       @id @default(autoincrement())
    username    String    @unique
    password    String
    fullname    String?
    dateOfBirth DateTime?
    gender      String?
    phoneNumber String?
    avatar      String?
}
